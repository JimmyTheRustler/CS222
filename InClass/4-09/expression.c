#include <stdio.h>
#include "stack.h"
#include "term.h"

double evaluate(Term term[], int size);

int main(){
	
	return 0;
}

double evaluate(Term term[], int size){
	Stack stack;
	
	stack.values = (double*)malloc(sizeof(double)*stack.capacity);
}
