#include <stdio.h>


int highestBit(unsigned int number):

int main(){
	
	
	return 0;
}


int highestBit(unsigned int number){
	if(number == 0){
		return -1;
	}
	
	int bit = 0;
	for(bit = 0; bit <= 31; bit++){
	
		if ( number &  (1 << bit) )
			
	
	}

}
